$(document).ready(function (){    $('#BookModal').submit(function (event){        event.preventDefault()        const DataForm = new FormData(this)        DataForm.append('action','store')        $.ajax({            url: '../controllers/bookingController.php',            type: 'POST',            data: DataForm,            contentType: false,            processData: false,            dataType: 'json',            success: function (res){                function ErrorMessages(field, message){ // helper function for showwing the error message                    if (message){                        $(`#${field}_msg`).html(message)                        $(`#${field}`).addClass('is-invalid')                        return                    }else {                        $(`#${field}_msg`).html('')                        $(`#${field}`).removeClass('is-invalid')                    }                }                if(res.errors){                    ErrorMessages('name',res.errors.name)                    ErrorMessages('StudentId',res.errors.StudentId)                    ErrorMessages('email',res.errors.email)                    ErrorMessages('date',res.errors.date)                    return;                }                if (res.success === true){                    Swal.fire({                        title: "Good job!",                        text: res.message,                        icon: "success"                    });                    setTimeout(() =>{window.location.reload()},2000)                }                if (res.success === false){                    Swal.fire({                        title: "Opps!",                        text: res.message,                        icon: "error"                    });                }            }        })    })})